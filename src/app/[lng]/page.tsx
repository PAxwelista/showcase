import { Header } from "@/components/client";
import { About } from "@/components/server";

export default function Page() {
    return (
        <main>
            <Header />
            <About />
        </main>
    );
}
